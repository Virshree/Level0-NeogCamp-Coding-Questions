// Write a JavaScript code snippet that achieves the following objectives:

// 1. Declare three variables named `fruit1`, `fruit2`, and `fruit3`, using the `let` keyword.
//  Initialize `fruit1` with the string value `"Apple"`, `fruit2` with the string value `"Banana"`, 
//  and `fruit3` with the string value `"Orange"`.
// 2. Declare another variable named `fruits` and assign it a string value obtained by concatenating
//  `fruit1`, `fruit2`, and `fruit3` with comma and space.
// 3. Print the value of the `fruits` variable to the console using the `console.log()` function.


let fruit1="Apple";
let fruit2="Banana";
let fruit3="Orange";

let fruits=fruit1 +" ,"+ fruit2 +", "+fruit3;


console.log(fruits);

// Apple ,Banana, Orange

//************************************************************** */
// 1. Declare three variables named `friend1`, `friend2`, and `friend3`, 
//using the `let` keyword.
//  Initialize `friend1` with the string value `"Sarah"`, `friend2` with the string value `"Alex"`,

//   and `friend3` with the string value `"Jessica"`.
// 2. Declare three variables named `greet1`, `greet2`, and `greet3`. Assign them string 
// values 
// by concatenating the respective friend names with the greeting `"Hey, "`.
// 3. Print the values of `greet1`, `greet2`, and `greet3` to the console using the `console.log()` 
// function.


let friend1="Sarah";
let friend2="Alex";
let friend3="Jessica";

greet1="Hey ,Sarah";
greet2="Hey ,Alex";
greet3="Hey ,Jessica";

console.log(greet1);
console.log(greet2);

console.log(greet3);

/*
Hey ,Sarah
Hey ,Alex
Hey ,Jessica
*/

//************************************************************** */

// 1. Declare three variables named `price1`, `price2`, and `price3`, using the `let` keyword. 
// Initialize `price1` with the numeric value `5.99`, `price2` with the numeric value `9.99`, and
//  `price3` with the numeric value `12.99`.
// 2. Print the values of `price1`, `price2`, and `price3` to the console along with an appropriate 
// message using the `console.log()` function.


let price1=5.99;
let price2=9.99;
let price3=12.99;

console.log("The price of the first item  is ",+price1);
console.log("The price of the second item  is ",+price2);
console.log("The price of the third item  is ",+price3);

/*
The price of the first item  is  5.99
The price of the second item  is  9.99
The price of the third item  is  12.99

*/